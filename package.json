{
  "name": "bonchi-v3",
  "version": "1.0.0",
  "description": "",
  "type": "module",
  "scripts": {
    "install-all": "bun install && cd src/client && npm install",
    "nvm": "nvm use 22.15.0",
    "nvm": "nvm use 22.15.0",
    "dev": "bun run --watch src/index.ts",
    "dev-server": "bun run --watch src/index.ts",
    "dev-client": "cd src/client && bun run dev",
    "build": "cd src/client && bun run build",
    "serve": "npm run nvm && npm run install-all && npm run db:generate && npm run build && bun run start",
    "start": "pm2 start ecosystem.config.cjs || pm2 restart Bonchi\\ Portal",
    "stop": "pm2 stop Bonchi\\ Portal",
    "test": "echo \"Error: no test specified\" && exit 1",
    "db:generate": "bunx prisma generate",
    "db:migrate": "bunx prisma migrate dev",
    "db:migrate:reset": "bunx prisma migrate reset",
    "db:pull": "bunx prisma db pull",
    "db:studio": "bunx prisma studio"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.965.0",
    "@prisma/client": "^7.2.0",
    "@prisma/extension-accelerate": "^3.0.1",
    "@types/cors": "^2.8.19",
    "@types/multer": "^2.0.0",
    "@types/multer-s3": "^3.0.3",
    "bcrypt": "^6.0.0",
    "cors": "^2.8.5",
    "crypto": "^1.0.1",
    "dotenv": "^17.2.3",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.3",
    "moment": "^2.30.1",
    "multer": "^2.0.2",
    "multer-s3": "^3.0.1",
    "uuid": "^13.0.0",
    "zod": "^4.3.5"
  },
  "devDependencies": {
    "@types/bcrypt": "^6.0.0",
    "@types/express": "^4.17.21",
    "@types/jsonwebtoken": "^9.0.10",
    "bun-types": "latest",
    "prisma": "^7.2.0"
  },
  "author": "",
  "license": "ISC"
}
